spring:
    application:
        name: backend

    thymeleaf:
        prefix: classpath:/templates/
        suffix: .html

mybatis:
    mapper-locations: classpath:/mapper/**/*.xml
    type-aliases-package: com.example.backend.domain
    configuration:
        map-underscore-to-camel-case: true
